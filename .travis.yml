language: java
jdk:
  - oraclejdk7
install:
  - true
script:
  - ./gradlew $TASK
env:
  global:
    - TERM=dumb
  matrix:
    - TASK=pullRequestValidation
    - TASK=check
branches:
  only:
    - master
    - travis-integration-test
sudo: false
